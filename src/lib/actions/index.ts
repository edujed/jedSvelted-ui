// Barrel export for action handlers (CRUD event/notification contract).
export { createHandleDetail } from '../handleDetailAction';
export type { HandleDetailOptions, ActionEvent } from '../handleDetailAction';
